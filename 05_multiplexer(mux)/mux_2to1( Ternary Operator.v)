// Code your design here
//Ternary Operator
module mux_2to1(a,b,s,y);
  input a;
  input b;
  input s;
  output y;
  
  assign y = s ? b:a; // Conditional Operator or Ternary Operator,
                      // condition ? true_value : false_value;
                      //          s?b:a;
endmodule
  
  
